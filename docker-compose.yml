services:
  api:
    build: .
    container_name: datamaq-communications-api
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    env_file:
      - .env
    ports:
      - "127.0.0.1:8000:8000"
